{
  "name": "portafolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "update": "git add . && git commit -m \"Actualiza proyecto\" && git push origin main && npm run build && npm run deploy"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^7.0.2",
    "gh-pages": "^6.3.0",
    "less": "^4.4.2",
    "vite": "^7.1.7",
    "vite-plugin-purgecss": "^0.2.13"
  }
}